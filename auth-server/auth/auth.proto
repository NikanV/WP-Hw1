syntax = "proto3";

option go_package = "./";

service rep_pq {
    rpc Authenticate (Request) returns (Reply) {}
}

message Request {
    string name = 1;
}

message Reply {
    string response = 1;
}